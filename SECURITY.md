# 보안과 개인정보

ResearchOps 1.x는 내부 신뢰 사용자의 조사 자동화용이다. 앱 로그인·역할·Task 소유권,
프록시 peer·Host·Origin·CSRF, 안전한 파일 반입, SMTP 전달 무결성과 중복 방지를 제공한다.
Native CLI가 같은 OS 계정으로 동작하는 운영 모드는 악성 Task를 위한 완전한 OS·네트워크·
디스크 quota 격리 경계가 아니다. 비신뢰 사용자가 임의 Task를 실행하는 공개 서비스로
운영하지 않는다. 상세는 [보안 계약](docs/12_SECURITY.md)을 따른다.

SMTP 비밀번호·주소 매핑·세션·DB·모델 인증정보는 애플리케이션의 보호된 운영 영역에
유지한다. Worker에는 수신자 그룹의 opaque ID/이름과 필요한 문서만 전달한다.
운영 설정·원본 Task·실제 로그·백업·실행 archive는 Git과 릴리스에 넣지 않는다.
문서의 계정·호스트·주소는 가상 값이며 예제 Task는 가상 소프트웨어 릴리스 조사이다.

취약점은 저장소 소유자와 기존의 비공개 연락 경로를 사용해 공유한다. 실제 토큰,
앱 비밀번호, 계정명, 내부 주소나 운영 Task 원문을 이슈·첨부·로그에 포함하지 않는다.
재현에는 합성 사용자와 독립 runtime을 사용하고 기대 결과·관찰 결과·영향 버전을 적는다.
활성 1.x 릴리스의 수정 사항은 [CHANGELOG](CHANGELOG.md)에 기록한다.

SMTP 최종 DATA 뒤 응답이 불확실하면 자동 재발송하지 않는다. SMTP 수락은 수신함 도착을
보장하지 않으며, 복구 시에도 이력과 원문을 보존한다.
